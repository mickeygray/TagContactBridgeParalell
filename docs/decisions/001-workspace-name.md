# Decision 001: Workspace Name

The new sibling workspace is named `TagContactBridgeParallel`.

Rationale:

- it is explicit about its relationship to the live `TagContactBridge` repo
- it signals side-by-side migration rather than in-place refactor
- it leaves room for later renaming once cutover planning is complete
