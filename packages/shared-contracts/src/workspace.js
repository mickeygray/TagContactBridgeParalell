"use strict";

const WORKSPACE_TOOLS = Object.freeze({
  admin: ["event-console", "service-health", "replay-controls", "metrics"],
  user: ["ringcentral-cx", "contact-history", "limited-text-email"],
});

module.exports = {
  WORKSPACE_TOOLS,
};
