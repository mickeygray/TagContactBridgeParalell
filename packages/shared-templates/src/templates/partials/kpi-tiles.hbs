<table class="kpi-row" cellpadding="0" cellspacing="0">
  <tr>
    {{#each tiles}}
      <td class="kpi {{tone}}">
        <div class="v">{{value}}</div>
        <div class="l">{{label}}</div>
      </td>
    {{/each}}
  </tr>
</table>
