"use strict";

// No business doctrine is published until DG-SOURCE-01 and the applicable
// per-rule DG-RULE-01 gates are approved.
module.exports = Object.freeze({
  id: "trainer-rule-registry",
  version: "1.0.0",
  status: "draft",
  testOnly: false,
  rules: Object.freeze([]),
});
