"use strict";

// Compatibility import for service-local consumers. The canonical contract
// lives in shared-config so the model and service layers share one source.
module.exports = require("../../shared-config/src/dailyReportContract");
