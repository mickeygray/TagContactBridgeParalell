# Self-adversarial pass — confirmed findings (2026-08-06)

Run `wf_4e822c3d-b79`. 7 attack lenses, each finding judged by 3 independent
skeptics; a finding is listed here only if 2+ said it was REAL.

**29 refute agents died on the session limit — the `test-mutation` lens was
hit hardest. Its findings are UNDER-judged: some were never voted on at all
and are therefore missing from this list. Re-run that lens.**

VERIFY EACH AGAINST CODE BEFORE FIXING. Both prior external audit rounds
contained findings that did not survive verification.


## [CRITICAL] Daily-chain double-send during the documented cutover: pass drain and gateway 09:00 sweep can send the SAME template twice to one lead
**counterCadenceService.js:347** · lens `cadence-drain-blast` · no dissent

**Scenario.** Transition state the gateway comment itself prescribes (arm MORNING_CADENCE_ENABLED first, flip OUTBOUND_DAILY_CADENCE_TO_PASSES only after observing): both the morning-pass drain and the gateway's 60s worker (includeDaily still true, window 09:00-11:59 PT) are live daily senders. The drain materializes its due list up front (selectCounterCadenceDueItems returns up to 200 stale .lean() snapshots), then dispatches sequentially over minutes. If the drain is still running past 09:00 — attempt 2/3 of the drained:false retry path, a control-plane restart at ~08:50, or the 45-min lease forcing a killed pass to resume ~09:35 — the gateway sweep (same sort: counterCadence.lastDispatchAt asc, so the SAME head leads) claims lead X's sms lock, sends daily sms-3, and recordCounterCadenceTouch UNSETS the lock and stamps lastDailyBatchKey. Two minutes later the drain reaches X in its pre-built list: the lock is gone, claimCounterCadenceItem succeeds, and it sends prospect-follow-up-text-3 AGAIN. Each gateway sweep during the overlap converts up to 25 leads into duplicate texts/emails; afterwards the DB is byte-identical to a single send (counter set to the same absolute value, same batch key), so the duplicates are invisible. The sms-2-vs-sms-3 same-tick pair specifically CANNOT both fire (age-relative needs counters.sms===1, daily sms needs >=2, and the touch writes counters+lastTouched atomically), but same-index double-fire can.

**Evidence.** claimCounterCadenceItem's filter is only `{ _id: item.lead._id, active: true, $or: [lock missing/null/expired] }` (lines 347-356) — it never re-checks `cadenceCounters.<channel>` or `counterCadence.lastDailyBatchKey.<channel>`, and dispatch then uses the stale snapshot: `dispatchForLead({ ...item.lead, ... })` (line 502-504). dailyAlreadyTouched is consulted only at evaluation time (line 303). The lock is a dispatch-duration mutex (unset by recordCounterCadenceTouch's `$unset: { counterCadence.locks.<ch> }`, line 424), not a day guard. Overlap is by design: apps/outbound-gateway/src/server.js:334-340 'it stays on until OUTBOUND_DAILY_CADENCE_TO_PASSES says a pass has taken it', while morningPassRuntime.js:117 forces the drain regardless of the 09:00 window, and passRuntimeFactory renews the lease only between tasks (advancePass, line 305), so a single long cadence apply can also be re-claimed by a second process mid-flight.


## [HIGH] No ownership re-check between claim CAS and the irreversible createTask — a stalled-but-alive holder and the drain can both create
**jiraWebhook.js:304** · lens `jira-claim-machine` · no dissent

**Scenario.** JIRA_TASK_BRIDGE_ENABLED=true. Webhook claims ASSIGNMENT-2100 at T0 and starts the bridge. In listOpenTasks' third window call, Logics returns headers, then the response BODY stalls — requestJson has already cleared its abort timer, so the await hangs unbounded and the row stays pending. At T0+10m the drain's CAS (matching only {outcome:'pending', lastAttemptAt}) succeeds — the predicate proves the holder hasn't WRITTEN, not that it died — and the drain re-runs the bridge from the snapshot; its createTask commits at ~T0+12m. At T0+13m the original run's stalled body finally completes carrying pre-create data; heldBySameOwner/alreadyOpen see no duplicate (their evidence was read before the drain's create), and the original calls createTask: a SECOND un-deletable Logics task on a live client case. The same shape works replay-vs-drain: if the drain's own bridge stalls past staleMs its row goes stale again and POST /replay/:key passes claimIssue's gate while the drain's create is still in flight.

**Evidence.** jiraWebhook.js:304-306 drain takeover: `const taken = await Model.findOneAndUpdate({ _id: claim._id, outcome: "pending", lastAttemptAt: claim.lastAttemptAt }, { $set: { lastAttemptAt: new Date(), ... }, $inc: { attempts: 1 } })` — a lease grant with no fencing at the point of irreversible effect: jiraTaskBridgeService.js:400 `const res = await deps.createTask(where.tenant, payload);` runs with no re-verification that the caller still owns the claim. The >10-minute stall is concretely reachable: packages/shared-integrations/src/httpClient.js:31-32 `clearTimeout(timer); const data = await parseResponse(response);` clears the abort timer BEFORE reading the body (parseResponse does `await response.text()`, httpClient.js:8), so a stalled body is an unbounded await — Logics HTTP calls are otherwise bounded (~20s timeout, logicsClient.js:107), which is why only this path (or an unbounded Mongo await in findLink/recordLink) keeps a live holder pending past STALE_CLAIM_MS (jiraWebhook.js:219). The SAME_WORK live-Logics dedupe backstops sequential retries but cannot see a create that commits after its windows were read.


## [HIGH] recordLink's non-created predicate carries no ownership token — the webhook catch block writes terminal `failed` over a claim the drain now owns
**jiraWebhook.js:194** · lens `jira-claim-machine` · no dissent

**Scenario.** Webhook claims at T0; its bridge stalls. Drain CAS-takes at T0+10m (lastAttemptAt=T1) and is mid-bridge with apply=true. At T0+12m the webhook's stalled call throws; the catch at jiraWebhook.js:415 calls recordLink({outcome:'failed'}), whose predicate {_id, outcome:{$ne:'created'}} matches the DRAIN-owned pending row — no lastAttemptAt CAS, unlike every takeover path. The row flips to terminal `failed` with a misattributed reason while the drain's createTask is in flight. Consequences: (1) `failed` is takeover-eligible, so a replay at T0+13m passes the claim gate and re-runs the bridge; its listOpenTasks can read Logics before the drain's create commits, producing a duplicate un-deletable task; (2) if the drain's bridge then throws, its catch writes nothing, leaving the ledger asserting `failed` for an attempt that never reported, with attempts inflated by a dispossessed writer. Symmetric hazard: the drain's own recordLink at line 313 can overwrite a webhook's FRESH re-claim if the drain's bridge run itself exceeded staleMs.

**Evidence.** jiraWebhook.js:192-194: `const query = rest.outcome === "created" ? { _id: jiraKey } : { _id: jiraKey, outcome: { $ne: "created" } };` — the only guard is against overwriting `created`. jiraWebhook.js:414-419 (webhook catch): `await deps.recordLink({ jiraKey: issue.key, outcome: "failed", reason: ..., trigger: event });`. The claim machinery arbitrates ownership via {outcome, lastAttemptAt} CAS everywhere else (claimIssue:246, drain:305), but terminal writes skip that arbitration entirely, so 'both believe they own the row' is realized at the terminal-write layer even though the takeover CAS pairs themselves are ABA-safe (lastAttemptAt only moves forward).


## [HIGH] All-held recovery pages return read=0/done=false, which buildDayQueue interprets as a stall and runDayStart aborts the morning dispatch
**leadDeliveryService.js:8275** · lens `recovery-cursor-and-paging` · no dissent

**Scenario.** Arm callRecoveryActivation.delivery with a recovery backlog whose head is non-admitting — guaranteed while the DNC sweep is unarmed (every episode holds 'dnc-unproven'), during any Logics outage (readCallRecoveryCaseState catches and returns null -> hold 'case-state-unproven'), or after day 7 of a cohort (see the dnc-stale finding). Morning sequence: runDayStart -> buildDayQueue drains the cadence pages (read>0 each), hands over to recovery, and the first pure recovery page lists 50 episodes, admits 0, so batch.items=[] while done=false and the cursor advances. processSourceRows returns read=0 (leadDeliveryService.js:4172 'read: rows.length' where rows=batch.items), buildDayQueue hits line 8275 and returns status 'queue-build-stalled', and runDayStart (8408-8419) returns early — dispatchMorningUntouched and the per-agent day-start packet posts (8422-8475) never run. Every subsequent minute tick retries runDayStart, whose buildDayQueue batch 1 is the next 50-episode all-held page -> read 0 -> abort again. The morning launch is delayed ~1 minute per 50 backlog episodes (a 2,000-episode 120-day backlog = ~40 minutes of idle agents in the 8am window), and each stalled attempt first burns 50 live Logics getCaseInfo calls. Pre-composite this branch was unreachable: cadence pages always have items.length === rows read, and an empty page sets done=true which line 8274 catches first.

**Evidence.** buildDayQueue: `if (Number(batch?.read || 0) === 0) return { ...summary, status: "queue-build-stalled", done: false };` (leadDeliveryService.js:8275), consumed by runDayStart: `const sourceRefresh = await buildDayQueue(); if (sourceRefresh.status !== "built") { ... return { status: sourceRefresh.status, ... } }` (8408-8419) — returning before dispatchMorningUntouched (8422). The zero-item page comes from callRecoveryCompositeSource.js readRecoveryBatch: `if (decision.decision !== "admit") continue;` (:121) with `const done = episodes.length === 0; return { items, nextCursor: done ? null : episodes[episodes.length - 1].episodeId, done }` (:128-133) — a page of 50 held episodes yields items:[] and done:false. Repair-lane wiring: `const batch = ... await source.readBatch({ cursor: repairCursor, ... })` then `const rows = Array.isArray(batch?.items) ? batch.items : []` (leadDeliveryService.js:4211-4214).


## [HIGH] readNewerBatch's held-head page loop performs up to 1,000 live Logics getCaseInfo calls per minute tick, inline in the tick, with no rate limit or cache
**callRecoveryCompositeSource.js:213** · lens `recovery-cursor-and-paging` · no dissent

**Scenario.** Once the daily repair completes, every delivery_open tick (floor 60s: server.js:1902-1905) runs ingestSerial in the new-arrivals lane (leadDeliveryService.js:9786, awaited inline in tick()). Cadence arrivals trickle, so cadenceItems.length < limit essentially always (composite:198) and the loop runs: 20 pages x recoveryPageSize 50 = up to 1,000 episodes, and for EACH episode resolveAdmissionInputs performs CaseProfile.findOne + ClientProfile.findOne + a live Logics HTTP getCaseInfo (server.js:1979-1984) + findItemBySourceIdentity (server.js:2074-2076), sequentially. With a persistent held head H (the dnc-stale mismatch keeps ~77-100% of open episodes unadmittable and unlinked, so they never leave the listing), every tick re-evaluates min(H,1000) episodes, discards every result, and admits nothing. At ~300ms per Logics call, H=500 makes one ingest pass ~2.5 minutes inside a 60-second tick — the tick body stretches, delaying productivityRebalance and runEndOfDayFolderDrain (9787-9788) every cycle, and Logics absorbs a sustained ~3-6 req/s all business day (~100k+ calls/day ceiling) from a path with no throttle, unlike the DNC sweep's deliberate 8/s cap (callRecoveryDncSweepService.js:27). callEndPulse (leadDeliveryService.js:5479-5493) runs the same full-head walk synchronously while an agent waits for a refill. The upstream code explicitly assumes this call is cheap: 'the minute tick remains bounded' (leadDeliveryService.js:8252-8253) — pre-composite readNewerBatch was one indexed Mongo query.

**Evidence.** `for (let page = 0; page < 20; page += 1) { const recovery = await readRecoveryBatch({ inner, limit: Math.max(1, want - recoveryItems.length), now });` (callRecoveryCompositeSource.js:213-218) with per-episode `inputs = await resolveAdmissionInputs({ episode });` (:113); wired to `const [profile, retained, payload] = await Promise.all([ CaseProfile.findOne(...), ClientProfile.findOne(...), createLogicsClient(domain).getCaseInfo(caseId) ]);` (apps/control-plane/src/server.js:1979-1984) plus `existingWorkItem: await leadDeliveryRepository.findItemBySourceIdentity(...)` (server.js:2074-2076). Gate that never blocks it: `if (!activation?.delivery || cadenceItems.length >= limit) return cadence;` (composite:198). Tick consumption: `: await ingestSerial());` (leadDeliveryService.js:9786) inside tick(), interval floored at 60s (server.js:1902-1905).


## [HIGH] Step-past after exhausted retries erases every durable trace of the failure; the day completes clean with undrained work
**passRuntimeFactory.js:322** · lens `pass-factory-semantics` · 1 skeptic dissented

**Scenario.** cadence-morning's drain loop hits its round cap or a stuck SMS provider three polls in a row and returns drained:false each time (line 282 throws it into the retry path). Attempt 3: `tried < maxTaskAttempts` (3<3) is false, so lines 322-327 step past — `index += 1` then advancePass, whose $set includes `[base+".lastErrorCode"]: null` (line 117). finishPass (line 151) also nulls lastErrorCode, and completion sets `state.lastError = null` (line 341). Durable result: passes.morning = { completedAt: <set>, counts: <final run only>, lastErrorCode: null } — indistinguishable from a clean day. The only trace of 'work remains due' is a log line and volatile state.lastResults, whose entry for the failed task is `results.push({ task, label, error })` (line 312) with NO applied/selected/failed counts, so even in memory the operator cannot see how much work remained; and if a LATER task then enters its own retry cycle, the final lastResults starts past the stepped-past task and loses even that error entry. The undrained leads silently lose their daily cadence touch; nothing re-attempts them today and nothing marks the day degraded. Worse: even DURING the retry loop the durable error marker barely exists — releasePass writes lastErrorCode (line 139) but the very next claimPass nulls it again (line 81), so it survives only the ~5-minute gap between release and re-claim.

**Evidence.** Line 117 (advancePass): `[`${base}.lastErrorCode`]: null,` — the step-past advance erases its own error marker. Lines 322-325: `// Out of attempts — step past it so the rest of the day still runs.\n index += 1;\n attempts.delete(task.key);\n const renewed = await advancePass(...)`. Line 341: `state.lastError = null;` on completion. Line 312: `results.push({ task: task.key, label: task.label, error: String(error.message).slice(0, 240) });` — applied counts discarded. Line 81 (claimPass): `{ $set: { [`${base}.claimedAt`]: at, [`${base}.lastErrorCode`]: null } }`.

**Dissent.** The pass-cursor facts are accurately quoted (passRuntimeFactory.js:322-327 steps past, advancePass:117 / finishPass:152 / claimPass:81 null lastErrorCode, :341 nulls state.lastError), but the headline claim — "erases every durable trace of the failure; the only trace is a log line and volatile state


## [HIGH] Weekend guard + hoist deletion silently skips the monthly filler-pool refresh and graduation sweep for any month whose 1st falls on Sat/Sun
**passRuntimeFactory.js:219** · lens `pass-factory-semantics` · 1 skeptic dissented

**Scenario.** Today the four floor services run on EVERY hourly tick, seven days a week (hourlySweeperService hoist), so a 1st-of-month falling on a weekend still fires the monthly filler refresh at 05:00 and the graduation sweep at 06:00. morningPassRuntime.js lines 22-29 record that the hoist will be DELETED in the same commit that arms the pass. After that, the only caller is the morning pass — and runOnce refuses Pacific weekends: `if (!force && !isPacificBusinessDay(now)) return { skipped: "pacific-weekend" };` (line 219). requireHour:false relaxes only the HOUR: isAtMonthlyRefreshBoundary still has `if (day !== "01") return false;` (fillerPoolRefreshService.js line 886), and the graduation sweep is gated by isFirstOfMonthPT's `day === "01"` (hourlySweeperService.js lines 796-803, used at 816). So when the 1st is a Saturday or Sunday (2026-08-01 WAS a Saturday; 2026-11-01 is a Sunday), the pass never runs that day, and on Monday the 2nd the day-check returns false — the month's filler-pool rebuild and the graduation sweep are skipped for the ENTIRE month, silently ({skipped: "not-at-monthly-boundary"} isn't even a failure, so no retry, no error, no lastErrorCode). The filler comment explicitly says the monthly burst is still load-bearing until the rolling refresh is verified ("Until then both run"). Separately: this same guard means dncRecheck, callrailStatSync and the aged daily ladder stop running Sat/Sun at all — a direct reversal of D11's default ("floor services yes" on weekends) that no ruling has approved. Also note: if floor-services happens to fail 3x on a weekday 1st (e.g. Mongo blip), the step-past path (finding 1) ALSO permanently forfeits that month's refresh — the boundary won't match on the 2nd.

**Evidence.** passRuntimeFactory.js line 219: `if (!force && !isPacificBusinessDay(now)) return { skipped: "pacific-weekend" };`. fillerPoolRefreshService.js lines 886-887: `if (day !== "01") return false;\n return requireHour ? hour === "05" : true;` — the day check survives requireHour:false. hourlySweeperService.js line 802: `return day === "01";` (isFirstOfMonthPT) and line 816: `if (isFirstOfMonthPT(now)) {` gating runMonthlyGraduationSweep. morningPassRuntime.js lines 26-28: "the hoist is deleted in the SAME commit that arms this pass".

**Dissent.** The claimed mechanism (passRuntimeFactory.js:219 weekend guard + hoist deletion) cannot produce the defect in this code. (1) The hoist is NOT deleted: hourlySweeperService.js:1270 still runs `summary.floor = await runFloorServices({...})` on every hourly tick, outside the scheduledPhase gate, and mo


## [HIGH] Drain loop has zero provider pacing for sms/email — the rate shaper has NO sms/email channels, and failed leads pin the head of every round
**counterCadenceService.js:805** · lens `cadence-drain-blast` · no dissent

**Scenario.** Morning pass, provider flaky (e.g., SMS API returning 429/500 with a trickle of successes — exactly what a throttling provider does). Round 1: 200 selected, 2 sent, 198 dispatch-exception failures. Failures write NO deferUntil (recordCounterCadenceFailure only defers on result.deferUntil/rateLimit.nextAllowedAt/'missing-tracking-number', lines 451-457) and never touch counterCadence.lastDispatchAt, so the 198 failed leads sort FIRST in the next candidate query and are re-selected seconds later. sent>0 and selected==perRound keep the loop running: 20 rounds x 200 = 4,000 back-to-back provider calls with no sleep between items or rounds, each failed lead hammered up to 20 times in minutes. drained:false then routes through the factory retry: 3 attempts x 20 rounds x 200 = up to 12,000 provider calls in one pass, per channel. The gateway path this replaces was naturally paced at <=25 dispatches per 60s tick. The sent===0 stop only saves the hard-down case (600 calls); any trickle of success defeats it — the 'RC-429 shape' the comment at lines 793-797 claims to prevent.

**Evidence.** drainCounterCadenceSweep (lines 805-825) is a bare `for` loop: `const r = await runCounterCadenceSweep(options);` with no delay; runCounterCadenceSweep's dispatch loop (lines 622-656) is sequential awaits with no pacing. The only dispatch-level throttle is evaluateOutboundRateLimit, but packages/shared-config/src/index.js:570-595 defines `outboundRateShaper.channels` for `rvm` and `cx` ONLY — resolveRateShaperChannelConfig returns null for sms/email and outboundRateShaperService.js:98-107 then answers `{enabled:false, allowed:true, reason:'rate-shaper-disabled'}`. Provider exceptions return `{ok:false, error, reason:'dispatch-exception'}` with no defer (outboundDispatchService.js:1319-1325). Sort key `counterCadence.lastDispatchAt: 1` (line 334) is only advanced by recordCounterCadenceTouch (line 409), never by the failure path. Gateway pacing for contrast: apps/outbound-gateway/src/server.js:256-267 (counterCadenceIntervalMs >= 60s, batch 25).


## [HIGH] Dispatch-level skips (rate-limited, quiet-hours, caps) are counted as FAILED — the midday rvm drain fights its own token bucket and burns the attempt budget every backlog day
**counterCadenceService.js:534** · lens `cadence-drain-blast` · no dissent

**Scenario.** Midday pass, 200+ rvm due (MIDDAY_CADENCE_MAX_DISPATCHES default 200). The rvm shaper allows ~40 tokens/15min (baseTokens 20, maxTokens 40). Round 1: 40 sent, 160 rate-limited. dispatchForLead returns those as {ok:false, result:{skipped:true, reason:'rate-limited', rateLimit}}, but dispatchCounterCadenceItem returns `{ok:false, item, result}` WITHOUT hoisting the nested skipped flag, so runCounterCadenceSweep's `failed` filter (`!entry.ok && !entry.skipped`) counts all 160 as failures. Each 'failure' writes recordCounterCadenceFailure: $inc cadenceState.failedByChannel.rvm, currentStage 'rvm-counter-failed', plus a workflow-stage 'failed' row — for what was correct pacing. deferUntil (~22s deficit) expires between rounds, so rounds 2-20 re-select and re-'fail' the same leads: one attempt accumulates thousands of phantom failure writes to send ~40-116 rvms. drained ends false, the factory throws 'did not drain — N failure(s)', retries 3x (5-min gaps refill ~13 tokens each), then steps past with the task marked failed. Outcome: every day the rvm backlog exceeds ~40, the midday cadence task reports failure, leads accrue bogus failedByChannel counts and 'rvm-counter-failed' stages, and workflow events get thousands of junk rows — while the actual chain advances only at the shaper's trickle.

**Evidence.** dispatchCounterCadenceItem's failure return (lines 534-539): `return { ok: false, item: summarizeDueItem(item), result: dispatchResult.result || dispatchResult };` — no top-level `skipped`, unlike the claim-conflict return (lines 492-498) which sets it. Sweep counters at lines 670-673: `failed: results.filter((entry) => !entry.ok && !entry.skipped)`. Rate-limit result shape: outboundDispatchService.js:1007-1015/1028 nests skipped inside `result`. Shaper caps: shared-config index.js:578-581 (rvm baseTokens 20, maxTokens 40, window 15min); capacity clamp in outboundRateShaperService.js:66-72. Per-failure lead writes: recordCounterCadenceFailure lines 459-479. Per-item workflow row: runCounterCadenceSweep lines 631-646 records stage 'failed' for every not-ok result.


## [HIGH] OUTBOUND_DAILY_CADENCE_TO_PASSES=true with the pass cadence tasks dark silently kills the entire daily chain — no guard, no warning, and partial arming kills single channels
**server.js:338** · lens `cadence-drain-blast` · 1 skeptic dissented

**Scenario.** Operator flips OUTBOUND_DAILY_CADENCE_TO_PASSES=true while MORNING_PASS_ENABLED/MORNING_CADENCE_ENABLED (and/or MIDDAY_*) are still false — the exact 'flag first' ordering nothing prevents. The gateway worker immediately passes includeDaily:false on every sweep, so daily sms, email AND rvm all stop; the passes are dark (runtime never starts) or standing-dry-run (writesArmed false → apply skipped), so nothing replaces them. First-touch and age-relative sms-2 keep flowing from the gateway, which masks the outage — leads keep getting text 1 and 2 and then silence forever. Partial arming is equally silent: morning armed but midday not → rvm chain alone dies. One gateway flag maps to two runtime flags plus two task flags across two processes (outbound-gateway vs control-plane, separately deployed .envs), and no code anywhere validates the combination, logs a mismatch warning, or documents it as forbidden outside a comment. Note the reverse ordering is finding 1's double-send window — there is no safe cutover order in code today.

**Evidence.** server.js:338-340: `includeDaily: String(process.env.OUTBOUND_DAILY_CADENCE_TO_PASSES || "false").toLowerCase() !== "true"` — read unconditionally, no check that any pass exists or is armed. The only cross-reference is the comment (lines 334-337). Grep for OUTBOUND_DAILY_CADENCE_TO_PASSES across the repo hits only this file, the work-order doc, and tests — no validation in control-plane, passRuntimeFactory (enabled default OFF, line 193: 'Deploying the code must never start a pass'), morningPassRuntime.js:98 (`writesArmed: () => flag("MORNING_CADENCE_ENABLED")`), or middayPassRuntime.js:31. passRuntimeFactory line 251 `if (armed && plannedCount)` skips apply entirely when the task flag is off.

**Dissent.** The claim's core premise — "there is no safe cutover order in code today" — is false, and with it the finding collapses to "a default-off handover flag can be flipped out of order," which is this repo's universal dark-launch property, not a defect. (1) Safe order exists: counterCadenceService.js:303


## [HIGH] isFactComplete is fed a queue-rollup range object, so the stored-rollup cache gate is always false and complete data is withheld
**reportComposerService.js:780** · lens `nightly-and-render-seams` · no dissent

**Scenario.** A range report longer than QUEUE_DAY_LOOP_MAX (7 days, e.g. a July monthly) with every day present in DailyQueueRollup: line 772 sets `stored = await readQueueRange({ from, to })`, whose coverage is {daysRequested, daysStored, missing, partialDays, unavailableDays, legacyDays, complete} (queueRollupService.js:255-266) — no capturedSections, no sectionErrors, no callProjection. isFactComplete (dailyReportFactService.js:99-106) computes `captured = new Set(fact.coverage?.capturedSections || [])` (empty) then `core.every((id) => captured.has(id))` (false) and `fact.coverage?.callProjection === "complete"` (undefined), so it returns false for EVERY possible readQueueRange result. The `if (isFactComplete(stored))` branch at :780 ("queue counts from N stored nightly rollup(s) — RingCentral not called") is unreachable dead code; a fully-covered range falls into the `daysStored > 0` branch at :782, which sets material.queueUnavailable = "only 30 of 30 day(s) captured" (:787) and advises INCOMPLETE (:796). Downstream, queueUnavailable makes reportBlocksService blank the call columns: `mailReadable = mailApplies && spendKnown && !queueUnavailable` (:486-487), `inboundCell = (x) => (d.queueUnavailable ? "—" : x.inbound)` (:673), and the work block prints "CALL DATA UNAVAILABLE for this range" (:2047-2049). Net: every long-range report renders complete stored queue data as unavailable, with a self-contradictory reason string.

**Evidence.** reportComposerService.js:772 `const stored = await readQueueRange({ from, to });` … :776-780 `const { isFactComplete } = require("./dailyReportFactService"); … if (isFactComplete(stored)) {`. isFactComplete (dailyReportFactService.js:100-105): `const captured = new Set(fact.coverage?.capturedSections || []); … const coreComplete = core.every((id) => captured.has(id)) && errors === 0; return coreComplete && fact.coverage?.callProjection === "complete";`. readQueueRange's coverage (queueRollupService.js:255-266) has only `daysRequested, daysStored, missing, partialDays, unavailableDays, legacyDays, complete` — the correct gate is the `complete: missing.length === 0 && partialDays.length === 0 && …` field computed at :262-265, which this code ignores. The message built at :787 reads "only ${daysStored} of ${daysRequested} day(s) captured" even when the two numbers are equal.


## [HIGH] Lease-renewal test is tautological — deleting the renewal passes 26/26
**passRuntime.test.js:398** · lens `test-mutation` · no dissent

**Scenario.** PROVEN BY MUTATION: removing `[base.claimedAt]: at` from advancePass's $set (apps/control-plane/src/services/passRuntimeFactory.js:114) — the exact audit fix this test exists to pin — leaves all 26 tests green. In production that mutation means the lease is fixed at claim time again: a pass whose tasks honestly exceed 45 minutes is reclaimed mid-write by a second process and the day runs twice (or, with real Mongo ms-equality, every multi-task day aborts CURSOR_LOST after task 1).

**Evidence.** The assertion is `assert.ok(new Date(cursor.claimedAt) >= new Date(cursor.completedAt) === false || cursor.nextTaskIndex === 3, "sanity")`. Precedence makes it `(claimedAt >= completedAt) === false || nextTaskIndex === 3`. claimedAt < completedAt is true BOTH with and without renewal (the claim stamp always precedes finishPass's completedAt), and the `|| nextTaskIndex === 3` escape is guaranteed by the very next line `assert.equal(cursor.nextTaskIndex, 3)`. No reachable world falsifies it. Ran `node --test tests/metrics/passRuntime.test.js` with the renewal deleted: pass 26 / fail 0.


## [MEDIUM] Bridge-returned `failed` is never retried by anything — the drain selects only `pending`, contradicting the model's own 'safe to retry' contract
**jiraWebhook.js:281** · lens `jira-claim-machine` · no dissent

**Scenario.** Flag armed. ASSIGNMENT-2200 is valid, verified, unheld. createTask hits one transient 20s timeout (Logics POSTs get timeoutMs 20000, retries 0 — logicsClient.js:107-108); bridgeJiraIssue catches it and returns a `failed` decision (jiraTaskBridgeService.js:407-412); recordLink parks the row as `failed`. Jira already received its 200 (sent before the bridge ran, jiraWebhook.js:365), so no webhook redelivery ever comes; the drain's query never selects `failed`; nobody edits the issue. The Logics task silently never exists and nobody is notified — recovery requires a human noticing in /links and replaying. Same terminal-parking applies to a thrown bridge error in the webhook path (unreadable open-task window): the catch records `failed`, despite the comment at jiraWebhook.js:122 claiming the throw lands in a retryable state.

**Evidence.** jiraWebhook.js:281: `const stuck = await Model.find({ outcome: "pending", lastAttemptAt: { $lt: cutoff } })` — the drain's only selector; grep shows no other writer ever re-drives `failed`. JiraTaskLink.js:42: `"failed", // Logics rejected it; safe to retry` and jiraWebhook.js:119-123: 'Throwing lands the decision in `failed`, which is retryable — the safe direction' — both comments describe a retry that does not exist. Only the DRAIN-path throw stays pending (drain catch at jiraWebhook.js:316 writes nothing) and self-heals; webhook-path transient failures are terminal in practice.


## [MEDIUM] attempts is a lifetime counter — takeover never resets it and every terminal write increments it, so successful skip cycles consume the drain's crash-recovery budget
**jiraWebhook.js:249** · lens `jira-claim-machine` · no dissent

**Scenario.** Dark mode. An issue is edited 4 times; each cycle is claim/takeover then recordLink skipped 'dry run — nothing written', and each recordLink $incs attempts: 1 (insert default) + 4 = 5. Fifth edit: takeover succeeds (attempts still 5 — the takeover $set carries no reset), then the process restarts before the terminal write. The drain finds the stale pending row and at line 295 (`Number(claim.attempts) >= maxAttempts`) immediately writes `failed` with reason 'drain gave up after 5 attempt(s)' — the drain made ZERO redrives; the crash-recovery machinery (claim + snapshot + drain) is denied precisely on the most-edited issues, and the recorded reason misstates what happened. Additionally each real drain redrive costs 2 attempts (CAS $inc at line 306 plus recordLink $inc at line 197), so even a clean pending row gets at most 2 redrives from a nominal budget of 5.

**Evidence.** jiraWebhook.js:248-252 takeover: `$set: { outcome: "pending", trigger, lastAttemptAt: new Date(), reason: "re-claimed", ...(snapshot ? { issueSnapshot: snapshot } : {}) }` — no attempts field. jiraWebhook.js:197 recordLink: `{ $set: { ...rest, lastAttemptAt: new Date() }, $inc: { attempts: 1 } }` — increments on every outcome including successful skips. jiraWebhook.js:295-299 gates the drain on the lifetime value and stamps `drain gave up after ${claim.attempts} attempt(s)` even when the drain itself attempted nothing.


## [MEDIUM] DNC 7-day reuse window vs 30/60/90-day recheck checkpoints leaves every episode held 'dnc-stale' for ~77% of its life, guaranteeing the permanent held head the composite re-walks
**callRecoveryAdmissionService.js:111** · lens `recovery-cursor-and-paging` · 1 skeptic dissented

**Scenario.** Episode checked clean at load-in on day 0: the sweep schedules the next check at day 30 (nextCheckpointAt walks CHECKPOINT_DAYS [30,60,90], callRecoveryDncSweepService.js:25,36-44,168-170), and listDncCheckpointsDue only surfaces rows with dnc.nextCheckAt <= now (callRecoveryLeadRepository.js:339-348). But admission rejects any clean result older than 7 days: DNC_REUSE_WINDOW_MS = 7 days (admissionService:37) -> hold('dnc-stale') (:111). So the episode is admittable days 0-7, then held with NO recheck booked until day 30 — and again days 37-60, 67-90, 97-120: ~28 admittable days out of 120. Concrete: episode eligibleFrom day 5, agent pools full days 5-7 -> it is never admitted while its DNC result is fresh, then sits unadmittable for 23 straight days although nothing changed. Fleet-wide this puts ~77%+ of open unlinked episodes permanently in the eligible/awaiting_start/held listing, which is exactly the head that the queue-build-stall finding aborts mornings on and the readNewerBatch cost finding re-scans with live Logics calls every tick. Either the reuse window must cover the checkpoint interval or a recheck must be booked at checkedAt+7d; as written the two constants cannot both be satisfied.

**Evidence.** `const DNC_REUSE_WINDOW_MS = 7 * DAY_MS;` (callRecoveryAdmissionService.js:37) and `if (now.getTime() - checkedAt.getTime() > DNC_REUSE_WINDOW_MS) return hold("dnc-stale");` (:111) versus `const CHECKPOINT_DAYS = Object.freeze([30, 60, 90]);` (callRecoveryDncSweepService.js:25) and `nextCheckAt: verdict.result === "clean" ? nextCheckpointAt(episode.firstQualifyingCallAt, now) : null` (:168-170); the due-list only fires at the checkpoint: `"dnc.nextCheckAt": { $ne: null, $lte: asOf }` (callRecoveryLeadRepository.js:343).

**Dissent.** The 7-vs-30 constant mismatch is real but the claimed scenario requires two impossible states and the fleet-wide conclusion fails. (1) A load-in check cannot predate eligibility: the sweep's population is `listEpisodesForConsideration` (callRecoveryDncSweepService.js:129-133) whose query filters `el


## [MEDIUM] The cadence-to-recovery handover drops batch.highWater, so the day's new-arrival boundary falls back to the recovery-completion time and rows created during the recovery walk are skipped for the rest of the day
**callRecoveryCompositeSource.js:148** · lens `recovery-cursor-and-paging` · no dissent

**Scenario.** Day where the cadence half finishes on its first page (active set <= limit, or empty — e.g. post-deactivation sweeps or a narrow sourceDomains deployment) while a recovery backlog spans additional ticks. readSourceBatch supplies highWater (newest row observed) on that page (leadDeliveryRepository.js:1077-1081), but the handover return builds a fresh object `{items, nextCursor, done}` without spreading batch — highWater is discarded, and since done=false (recovery still paging), ingestDurableSourceOnce persists nothing (`if (!highWater && batch?.highWater)` sees undefined; the `else if` needs done===true, leadDeliveryService.js:4229-4239). The following ticks are recovery-kind pages that also carry no highWater. When recovery finally completes N minutes later, the fallback stamps highWaterCreatedAt = at (the FINAL tick's time) with id '000000000000000000000000'. Any LeadCadence row created between the cadence drain and that final tick — precisely a fresh lead the direct intake path dropped, the case this repair lane exists for — sits below the boundary and readSourceNewerBatch (`createdAt > highWater.createdAt`, leadDeliveryRepository.js:1103-1106) never returns it. It is not ingested until the next Pacific day's full repair walk: a perishable purchased lead delayed ~a day, silently. Combined with the all-held stall finding, the unprotected window is the entire ceil(H/50)-minute morning walk. Pre-composite the same done page kept highWater via `{...batch}` and this window did not exist.

**Evidence.** Handover branch: `const recovery = await readRecoveryBatch({ inner: null, limit, now }); return { items: [...(batch.items || []), ...recovery.items], nextCursor: recovery.done ? null : encodeCursor(RECOVERY_KIND, recovery.nextCursor), done: recovery.done };` (callRecoveryCompositeSource.js:147-152) — contrast the sibling branches that preserve it: `return { ...batch, nextCursor: ..., done: false }` (:142) and `return { ...batch, nextCursor: null, done: true }` (:146). Consumer: `if (!highWater && batch?.highWater) { set.highWaterCreatedAt = batch.highWater.createdAt; ... } else if (!highWater && batch?.done === true) { set.highWaterCreatedAt = at; set.highWaterId = "000000000000000000000000"; }` (leadDeliveryService.js:4229-4239).


## [MEDIUM] No durable attempt bound: a restart cycle grants the failing task fresh attempts forever, hammering providers and blocking the rest of the pass
**passRuntimeFactory.js:211** · lens `pass-factory-semantics` · no dissent

**Scenario.** attempts is `const attempts = new Map()` per instance (line 211) and nothing in passes.<passKey> persists an attempt count — only nextTaskIndex/counts/lastErrorCode (DailyLoopRun.js line 79-80 documents the shape). The comment states one restart SHOULD grant fresh attempts. But the pass stack runs under nssm (restart-on-exit): if cadence-morning totalFailures (provider outage → synthetic throw at line 269) and the control-plane service is in a restart cycle — a crashing sibling runtime, a flapping deploy, a reboot loop — every restart resets the Map while releasePass has left claimedAt null and the cursor parked at the failing index. Each new process immediately re-claims (start() calls runOnce() at line 366) and re-runs the failing task with a fresh 3-attempt budget: full drain batches against the SMS/RVM provider, every cycle, all day. The step-past escape (line 322) is only reachable after 3 consecutive failures WITHIN one process lifetime, so a restart cadence shorter than ~15 minutes means the pass never steps past: call-log-hygiene-midday and every later task never run that day, the day-doc never completes, and provider bursts continue until midnight rolls the dateKey. If the failure itself crashes the process mid-task, the stale-claim lease bounds it to one burst per 45 minutes (line 68) — still unbounded in count across the day.

**Evidence.** Line 211: `const attempts = new Map();` with the comment `a restart SHOULD grant fresh attempts because a restart is itself a plausible cause of the failure` — true for ONE restart, unbounded for a cycle. Line 139 (releasePass): `$set: { [`${base}.claimedAt`]: null, ... }` — immediate re-claim after restart. Line 366: `await runOnce();` inside start(). No attempts field anywhere in the durable cursor (DailyLoopRun.js passes shape: `{ claimedAt, completedAt, nextTaskIndex, counts, lastErrorCode }`).


## [MEDIUM] forceDaily bypasses the weekday guard end-to-end: a forced weekend run sends the full daily EMAIL chain on Saturday (exposed today via the gateway manual route)
**counterCadenceService.js:280** · lens `cadence-drain-blast` · 1 skeptic dissented

**Scenario.** POST /api/outbound/counter-cadence/run with {"forceDaily": true} on a Saturday (internal-access only, no weekday check on the route — the gateway's isPacificBusinessDay guard lives only in the worker tick), or a future runOnce({force:true}) on a pass (factory line 219 skips the pacific-weekend refusal under force; line 239 additionally lets apply:true bypass MORNING_CADENCE_ENABLED entirely). evaluateCounterCadenceDueItems sets dailyAllowed = forceDaily || ..., so isWeekdayBatchTime — the ONLY place COUNTER_CADENCE_ACTIVE_WEEKDAYS is consulted — never runs. The last backstop is per-dispatch contactTimingPolicy: sms/rvm get deferred with reason 'off-weekday' (each writing a phantom counter-failure: failedByChannel +1, currentStage 'sms-counter-failed', deferUntil Monday — hundreds of junk failure writes per forced weekend run), but EMAIL is explicitly restricted:false (TCPA-exempt, any day, any hour), so every email-due lead receives its next chain email on Saturday. The old code path could never do this: the 09:00-window check gated all three channels on ACTIVE_WEEKDAYS unconditionally.

**Evidence.** counterCadenceService.js:280 `const dailyAllowed = forceDaily || (includeDaily && isWeekdayBatchTime(now));` — ACTIVE_WEEKDAYS only inside isWeekdayBatchTime (lines 120-126). Gateway route: apps/outbound-gateway/src/server.js:604-625, `forceDaily: Boolean(req.body?.forceDaily)` with no business-day check. Pass factory bypasses: apps/control-plane/src/services/passRuntimeFactory.js:219 (`if (!force && !isPacificBusinessDay(now))`) and 239 (`const armed = apply === null ? task.writesArmed() : Boolean(apply)`). Email exemption: contactTimingPolicyService.js:151-161 returns `restricted: false` for email while sms/rvm default activeWeekdays '1,2,3,4,5' (line 136). Off-weekday sms defer → recordCounterCadenceFailure counts and stage writes at counterCadenceService.js:459-479.

**Dissent.** The claim's novelty assertion is false and its blast radius contradicts the code. (1) Not introduced by this branch: master (v0.0.1 bed8640) already has the identical line — `git show master:packages/shared-services/src/counterCadenceService.js` line 257: `const dailyAllowed = forceDaily || (include


## [MEDIUM] Claim-conflict skips are invisible to the drained certification: a stale lock makes the loop certify a drained backlog, and pure contention is mislabeled 'stuck provider'
**counterCadenceService.js:819** · lens `cadence-drain-blast` · no dissent

**Scenario.** Case A (false drained): a prior drain attempt is killed mid-dispatch (nssm stop timeout, passRuntimeFactory.stop waits only 25s), leaving one lead's channel lock held for the 10-min CLAIM_TTL. The retry attempt re-selects that lead every round (evaluate/selection never read counterCadence.locks), claim-conflicts it into `skipped`, and when the final round selects fewer than perRound the loop runs `totals.drained = totals.failed === 0` — skipped>0 does not block it — so the day completes 'drained' while the lead's daily item was never sent and nothing revisits it until tomorrow. Case B (false stuck-provider): a round whose selections are all claim-conflicts (contention with the concurrently-running gateway sweep during the finding-1 overlap) has sent===0, hits the 'stuck provider' break with drained still false, and the factory then throws 'did not drain — 0 failure(s)' and burns up to 3 retry attempts on lock contention with no provider problem at all. Answering the brief directly: claim failures count as skipped (not sent, not failed); contention alone cannot make the loop SPIN (sent===0 breaks), but it can both falsely certify drained and falsely diagnose a stuck provider. Side effect: totals.selected double-counts the same conflicted/failing lead across rounds, so the day summary can report 'selected 4000' for 200 distinct leads.

**Evidence.** Stop-1 at lines 819-822: `if ((Number(r?.selected) || 0) < perRound) { totals.drained = totals.failed === 0; break; }` — no reference to r.skipped even though line 811 accumulates it. Stop-2 at line 824 comments 'A full selection that sent nothing is a stuck provider' but claim-conflicts also produce sent 0. Claim-conflict shape: lines 492-498 (`ok:false, skipped:true, reason:'claim-conflict'`); sweep buckets it as skipped only (lines 672-673). Neither buildCandidateQuery (lines 321-330) nor evaluateCounterCadenceDueItems filters on counterCadence.locks, so locked leads are re-selected every round; CLAIM_TTL_MS is 10 min (line 15). The factory turns drained:false into a thrown retry at passRuntimeFactory.js:282-286.


## [MEDIUM] The facts.calls placeholder skip only checks status "pending" — an errored longcalls section writes {status:"unavailable"} over real stored counts
**dailyReportFactService.js:237** · lens `nightly-and-render-seams` · 1 skeptic dissented

**Scenario.** Day D's fact holds real call counts in facts.calls (posted by the daily-entry worker/script or attachDailyCallFacts). D is re-written through the snapshot path (same --date re-send seam as above, or a same-night retry) on a night CallRail is down, so the composed report's longcalls section carries an error. buildDailyReportFact then computes `callProjection = callFacts ? "complete" : (callSection?.error ? "unavailable" : "pending")` (:158-160) and the calls slot becomes `{ status: "unavailable", rowsObservedInEmail: 0 }` (:188-190). The guard at :237 — `if (key === CALL_SECTION_FACT && value && value.status === "pending") continue;` — matches only "pending", so the "unavailable" marker is $set over facts.calls, destroying the only real numbers in the document. The comment directly above (:230-233) claims "What it writes here is a marker, {status:\"pending\"}" — false: the marker can also be "unavailable", and that one overwrites. This is data destruction, not just metadata regression, and the write is stated by the same comment to be the thing this guard exists to prevent ("Writing that marker over counts the worker already stored would destroy the only real numbers in the document").

**Evidence.** dailyReportFactService.js:158-160 `const callProjection = callFacts ? "complete" : (callSection?.error ? "unavailable" : "pending");` and :188-190 `calls: callFacts ? sanitizeFactValue(callFacts) : { status: callProjection, rowsObservedInEmail: sectionRowCount(callSection) }` versus the skip at :237 `if (key === CALL_SECTION_FACT && value && value.status === "pending") continue;`. An "unavailable" status flows through to `set["facts.calls"]` at :238. The guard should skip any non-callFacts marker (both "pending" and "unavailable"), or key the skip on the absence of real callFacts rather than on one status string.

**Dissent.** The guard at dailyReportFactService.js:237 does match only "pending" while :158-160 can produce "unavailable" — but the claimed destruction requires real call counts already stored in facts.calls, and no code in this tree can put them there. The three writers the scenario names: (1) attachDailyCallF


## [MEDIUM] The nightly hygiene claim lease (45 min) is never extended mid-pass, while the pass's own sizing math exceeds it — mutual exclusion is lost mid-task on a fully-armed night
**nightlyHygieneRuntime.js:46** · lens `nightly-and-render-seams` · 1 skeptic dissented

**Scenario.** A fully-armed pass starts at 19:50: claimNightlyHygiene stamps nightlyHygieneClaimedAt once (:194) and advanceNightlyHygiene never refreshes it (:210-221 $sets only nextTaskIndex/counts). The claim query re-admits any claimant once claimedAt < now - 45min (:176, :189). The pass's own comments size payment-reconcile ALONE at the whole lease ("500 × 3 is ~1,500 serial calls, which fits the lease with room for the timeout tail", :1343-1345 — at logicsClient's 20s timeout a modest tail alone eats the room), and it is task 11 of 15, behind night-persist's full runNightPass dry gather (task 0), a mailbox visit, call-recovery-discovery's gather that re-pulls a 45-day CallRail window even for one day (:369-371), a DNC sweep of up to 100 RPV + 200 serial Logics getCaseInfo calls (:974-979), and the standing dry-run that runs every plan() even unarmed (:1846-1852, priced at "eleven plan() calls" though there are now fifteen tasks). At minute 46+, a restarted control-plane (nssm restart, deploy) or a second instance re-claims the day and resumes at the stored nextTaskIndex — the SAME task the first instance is still executing, since the cursor advances only after task completion. Both run e.g. payment-reconcile concurrently against live Logics (double serial pulls, racy "write only rows we do not have" inserts); the first instance detects CURSOR_LOST only at its next advance (:1869-1871, :1886-1888), after the in-flight task finishes. This is the identical never-extended-lock defect the same file documents as a flaw in caseProfilePaymentSyncService (:1429-1431: "the lock is never extended … so a run longer than its TTL loses mutual exclusion").

**Evidence.** `const HYGIENE_CLAIM_LEASE_MS = 45 * 60 * 1000;` (:46); claim admits stale leases via `{ nightlyHygieneClaimedAt: { $lt: leaseCutoff } }` (:189); advanceNightlyHygiene's $set (:213-217) contains no claimedAt refresh, so the lease measures from pass START, not from last progress. Duration evidence in-file: :1338-1346 (payment-reconcile lease math), :360-372 (discovery gather re-pulls 45 days of CallRail per gather), :974-979 (limit 100 RPV sweep + limit 200 Logics check, serial), :1843-1845 ("some of them are not cheap — mail-invoice opens a mailbox, call-recovery-discovery consumes a full gather"). The one-line fix shape already exists in the codebase's own critique: refresh claimedAt inside advanceNightlyHygiene.

**Dissent.** The never-extended-lease-vs-pass-duration defect is already recorded as arming blocker §3-B in .ai/context/EVENING_PASS_ARMING_RUNBOOK_2026-08-06.md:159-168 — "### B. Nothing bounds a task's wall clock *(high)* / No timeout wraps `task.plan()` or `task.apply()`, and the 45-minute lease is only consu


## [MEDIUM] 'Each handler is included per ITS OWN flag' is false — the invoice handler has no flag; NCOA's flag alone arms live invoice writes
**nightlyHygieneRuntime.js:550** · lens `comment-claims-audit` · no dissent

**Scenario.** Set NCOA_MAILBOX_ENABLED=true and MAIL_INVOICE_MAILBOX_ENABLED=false (operator believes invoice ingestion stays dark, as the comment promises). writesArmed() (lines 705-709) returns true on EITHER flag; buildMailboxHandlers (lines 559-570) includes createMailInvoiceHandler UNCONDITIONALLY — only the NCOA handler is behind `if (ncoaEnabled)`. apply() then calls readMailInvoiceMailbox({apply:true}), so the invoice handler runs with apply=true: mailInvoiceMailboxHandler.js:265 shows only `if (!apply)` returns dry-run, i.e. with apply=true it writes invoice ingest records and runMailboxIngest.recordHandled stamps the message handled — live writes from a task whose own flag is off.

**Evidence.** Comment at nightlyHygieneRuntime.js:550-553: 'Each handler is included per ITS OWN flag. NCOA's flag gated runNcoaMailboxIngestIfDue before this fold and it gates inclusion here...'. Code at 567-568: `const handlers = [createMailInvoiceHandler({ targetDate: ... })]; if (ncoaEnabled) handlers.push(createNcoaHandler());` — no invoice flag anywhere in the inclusion path, and grep of mailInvoiceMailboxHandler.js shows no MAIL_INVOICE_MAILBOX_ENABLED read (only MAIL_INVOICE_GMAIL_QUERY at its line 128). The claim is true for NCOA only.


## [LOW] The drain and replay paths drop the decision.notify side effect — only the webhook path ever comments back on Jira
**jiraWebhook.js:313** · lens `jira-claim-machine` · no dissent

**Scenario.** Flag armed. Webhook claims ASSIGNMENT-2300, host restarts after the 200. The drain redrives from the snapshot; the bridge returns skipped with notify set ('Monica Cazares already has an open Logics task ... on this case'). The drain calls deps.recordLink(decision) and moves on — decision.notify is never examined, so no comment is posted to the Jira issue. Per the code's own rationale (jiraWebhook.js:149-151, 388-390), the reporter 'hears nothing and reasonably concludes the bridge dropped it'. The replay path has the identical gap: POST /replay/:key returns the decision JSON to the admin but posts no comment for the reporter.

**Evidence.** Drain: jiraWebhook.js:310-315 — `const decision = await bridge({...}); await deps.recordLink(decision); out.redriven += 1;` with no `decision.notify` handling. Replay: jiraWebhook.js:453-457 — `const decision = await bridgeJiraIssue({...}); await deps.recordLink(decision); return res.json({ ok: true, decision });`. The notify contract is implemented only in the webhook handler at jiraWebhook.js:391-409 (`if (decision.notify) { ... await deps.commentOnIssue(issue.key, text); }`).


## [LOW] A refused concurrent delivery is 200-acked and dropped, so an issue_updated arriving during the drain's ownership window is lost and the drain records a decision from the outdated snapshot
**jiraWebhook.js:366** · lens `jira-claim-machine` · 1 skeptic dissented

**Scenario.** Crash at T0 leaves ASSIGNMENT-2400 pending with a snapshot whose summary lacks a case id. Drain CAS-takes at T0+10m and starts a multi-minute bridge run against the T0 snapshot. At T0+11m a user fixes the summary and Jira fires issue_updated; the webhook's claimIssue reads pending-FRESH (the drain just bumped lastAttemptAt) and returns {ok:false, reason:'another delivery holds the claim'}; the handler responds 200 with claimed:false and returns — Jira treats it delivered and never redelivers. Drain completes at T0+12m and records skipped 'no case id in the summary' from the stale snapshot. The fixed payload is gone; /links now shows a skip reason the current issue no longer exhibits, and nothing reprocesses until a human edits the issue yet again or replays.

**Evidence.** jiraWebhook.js:365-369: `res.json({ ok: true, received: event, key: issue.key, claimed: claim.ok }); if (!claim.ok) { logger?.info?.("jira.webhook.already_handled", ...); return; }` — refusal is an ack, not a 409/5xx, so redelivery is off. The drain re-drives `claim.issueSnapshot` (jiraWebhook.js:310-311), frozen at original claim time; the fresher payload that lost the claim race is discarded rather than queued or used to refresh the snapshot.

**Dissent.** The scenario needs the drain to still hold a pending claim at T0+11m on a no-case-id snapshot, but that path has no multi-minute run: jiraTaskBridgeService.js:220 `if (!caseId) return { tenant: null, reason: "no case id in the summary" };` returns before any fetchCase/listOpenTasks network call, so 


## [LOW] claimIssue takeover never clears prior-cycle decision fields, so the ledger can show a self-contradictory row (old tenant/subject glued to a new reason)
**jiraWebhook.js:248** · lens `jira-claim-machine` · no dissent

**Scenario.** Cycle 1 (dark): decision skipped 'dry run' stores tenant TAG, caseId 401656, subject 'Prep Return', userNames. A user then mangles the summary; cycle 2's decision is skipped {reason:'no case id in the summary', caseId:null} — tenant, subject and userNames are ABSENT from that decision, and recordLink's $set only overwrites keys present in it, while the takeover $set (outcome/trigger/lastAttemptAt/reason/issueSnapshot only) cleared nothing. The /links row now reads outcome=skipped, reason='no case id in the summary', tenant=TAG, subject='Prep Return', owners populated — an admin triaging why the ticket produced no task trusts fields that belong entirely to a superseded cycle.

**Evidence.** Takeover $set at jiraWebhook.js:248-252 touches only `{ outcome, trigger, lastAttemptAt, reason, issueSnapshot? }`; recordLink at jiraWebhook.js:197 does `$set: { ...rest, lastAttemptAt: new Date() }` where rest is the new decision's keys only — e.g. the no-case-id skip decision is built at jiraTaskBridgeService.js:320 as `done("skipped", { reason: where.reason, caseId: parsed.caseId })` with no tenant/subject/userNames to overwrite the stale ones.


## [LOW] The recovery sentinel cursor {createdAt: 1970-01-01, id: 'recovery:<episodeId>'} leaks unmasked into runtimeState.sourceCursor and the getState() health surface
**leadDeliveryService.js:4257** · lens `recovery-cursor-and-paging` · no dissent

**Scenario.** Mid-recovery, ingestDurableSourceOnce copies the persisted repair cursor into runtimeState.sourceCursor verbatim, and getState() exposes it (leadDeliveryService.js:9899). Any operator, dashboard, or monitoring probe that reads sourceCursor.createdAt as a progress timestamp — including the 'newest-write probe' diagnostic pattern this shop uses to decide whether a runtime is alive — sees an epoch date and concludes the source cursor is 56 years stale, and the raw episode id (domain:caseId embedded in 'recovery:callrail-long-call-recovery:TAG:12345:1') leaves the count-only boundary the recovery program enforces elsewhere (server.js:1969 'Count-only, per §23 — never a name, phone, case id or call id'). No in-repo reader currently rejects or resorts on the epoch value (verified: the only consumers of repairCursorCreatedAt/repairCursorId are ingestDurableSourceOnce and this observability copy; requireDate accepts Date(0); nightlyHygieneRuntime never touches source_repair checkpoints), so this is a diagnosis/PII-hygiene hazard rather than a control-flow bug — but it is the one place the codec's smuggled shape escapes the codec.

**Evidence.** `runtimeState.sourceCursor = updated.repairCursorCreatedAt && updated.repairCursorId ? { createdAt: updated.repairCursorCreatedAt, id: updated.repairCursorId } : null;` (leadDeliveryService.js:4257-4259) surfaced by `sourceCursor: runtimeState.sourceCursor,` in getState() (:9899); sentinel minted at `return { createdAt: new Date(0), id: `${RECOVERY_ID_PREFIX}${inner}` };` (callRecoveryCompositeSource.js:72). Also verified safe, for the record: listEpisodesForConsideration paging is strictly-greater keyset on the sort key itself (`query.episodeId = { $gt: String(after) }` with `.sort({ episodeId: 1 })`, callRecoveryLeadRepository.js:331-334), so the no-progress guard's `recovery.nextCursor === inner` (composite:230) is unreachable from a correct listing — nextCursor is null only when done, and a returned last id is always > after.


## [LOW] attempts Map never cleared on day rollover or when another process finishes the day — retry budget silently shrinks on later days
**passRuntimeFactory.js:310** · lens `pass-factory-semantics` · no dissent

**Scenario.** attempts.delete(task.key) happens only on task success (line 304) or step-past (line 324) — both inside a runOnce that still holds the claim. The retry return (line 320) intentionally leaves the count set for same-day resumption, but nothing keys it by dateKey or clears it when the day ends elsewhere. Two-instance interleave (the exact deployment the lease machinery exists for): instance A runs cadence-morning, it fails once → attempts.set(key, 1) → releasePass. Instance B wins the next claim and completes the task (or exhausts ITS OWN counter and completes the day). A's Map entry stays at 1 forever — A never re-enters the loop for that task (claimPass returns null once completedAt is set). NEXT day the provider hiccups while A holds the claim: `tried = (attempts.get(key) || 0) + 1` (line 310) starts at 2, so A steps past after only 2 real attempts; with a stale count of 2, a single failure steps past immediately. Same leak in one process if a day ends mid-retry (runtime stopped/disabled overnight, or the failing task's day rolls over at midnight). The 3-attempt budget the comments promise degrades invisibly.

**Evidence.** Lines 310-311: `const tried = (attempts.get(task.key) || 0) + 1;\n attempts.set(task.key, tried);` — keyed by task.key alone, no dateKey. Deletes only at lines 304 and 324. No clearing on `skipped: "claimed by another pass"` (line 229) or on dateKey change (line 217).


## [LOW] 'Arming decides whether the mailbox is opened at all' is false — the standing dry run opens Gmail nightly regardless of arming
**nightlyHygieneRuntime.js:555** · lens `comment-claims-audit` · no dissent

**Scenario.** An operator leaves both MAIL_INVOICE_MAILBOX_ENABLED and NCOA_MAILBOX_ENABLED off and, per this comment, expects the shared mailbox to never be opened. In fact runOnce() executes task.plan() for every task whether or not it is armed (lines 1833-1847), and mail-invoice's plan() (lines 710-711) calls readMailInvoiceMailbox({apply:false}), which opens the Gmail client and lists/downloads messages every night NIGHTLY_HYGIENE_ENABLED is on. A security review of Gmail access, or a quota budget, based on this comment reaches the wrong conclusion.

**Evidence.** Comment at nightlyHygieneRuntime.js:555-557: 'arming decides whether the mailbox is opened at all, this decides what is read once it is.' Contradicted by the runtime's own loop comment at 1844-1846: 'eleven plan() calls a night whether or not anything is armed, and some of them are not cheap — mail-invoice opens a mailbox' and by the code at 1847 (`const planned = await task.plan(...)` runs before the `!armed` branch).


## [LOW] persistTargetDay JSDoc and night-persist comment still describe the dead 02:00 design (offset -1); code runs 19:50 with offset 0
**nightlyHygieneRuntime.js:250** · lens `comment-claims-audit` · 1 skeptic dissented

**Scenario.** The JSDoc says 'Offset -1 by default: at the 02:00 default hour the current Pacific day is only two hours old', so a reader expects the pass to persist YESTERDAY. The code defaults NIGHT_PERSIST_DAY_OFFSET to 0 (line 260) and DEFAULT_HOUR/DEFAULT_MINUTE are 19/50 (lines 39-40), so the persisted dateKey is TODAY. An operator wiring a consumer to read the 'yesterday' key reads an empty day; one who sets NIGHT_PERSIST_DAY_OFFSET=-1 'to match the documented default' shifts every night-persist snapshot onto the wrong day. The night-persist task repeats the stale claim at lines 611-612: 'This service runs at 02:00, where pacificDateKey() is a day only two hours old'.

**Evidence.** nightlyHygieneRuntime.js:250-252 (JSDoc): 'Offset -1 by default: at the 02:00 default hour...'. Contradicted three lines later by the inline comment (256-259, 'Offset 0 because the pass runs in the EVENING: at 19:50...') and the code at 260: `const offset = Number(process.env.NIGHT_PERSIST_DAY_OFFSET ?? 0);` plus `const DEFAULT_HOUR = 19; const DEFAULT_MINUTE = 50;` at 39-40. Same stale 02:00 claim again at 611-612 inside the night-persist task's plan().

**Dissent.** Stale-prose only; code behavior is correct and self-correcting at the point of configuration. apps/control-plane/src/services/nightlyHygieneRuntime.js:255-260 — the function body's own comment, placed directly on the lines that set the default, states the current design: "// Offset 0 because the pas


## [LOW] 'MD2, not built yet' — the midday call-log-hygiene half exists in middayPassRuntime.js
**nightlyHygieneRuntime.js:161** · lens `comment-claims-audit` · 1 skeptic dissented

**Scenario.** An engineer asked to cover the previous-evening-to-midday call window reads 'the midday pass (MD2, not built yet) covers the previous evening → midday', concludes the half does not exist, and builds a second one — duplicating a pass whose overlap semantics are explicitly non-idempotent (the E8/midday comments document that an overlapping replay rewrites resolved CallLog rows to pending-retry and duplicates review-queue rows).

**Evidence.** nightlyHygieneRuntime.js:160-161: 'the evening pass covers 12:00 → now and the midday pass (MD2, not built yet) covers the previous evening → midday'. Contradicted by apps/control-plane/src/services/middayPassRuntime.js:75-183, which contains the built task `key: "call-log-hygiene-midday"` anchored at `pacificMsSinceToday(20, 0)` (line 101) — including its own arming-blocker warning block, i.e. built and dark, not unbuilt.

**Dissent.** The factual predicate is true but the defect is negligible: it is a stale phrase in a comment attached to a constant, with zero runtime effect and its misleading power already neutralized by accurate cross-references in the very files an engineer would touch. The stale text is nightlyHygieneRuntime.


---

## Dismissed by the panel (recorded so nobody re-raises them)

- low/jira-claim-machine: Replay 409 does not say who holds the claim or since when — only a generic reason string
- high/pass-factory-semantics: Persisted counts and lastResults reflect only the final runOnce invocation — work completed before a retry release is erased from the day's record
- high/pass-factory-semantics: Unvalidated hour/minute env parse can schedule the pass at midnight PT, and forceDaily bypasses the only cadence time gate — quiet-hours SMS/RVM sends
- medium/pass-factory-semantics: Weekend guard leaves Friday-after-08:00 and all of Saturday permanently unsynced in the callrailStatSync floor feed
- low/pass-factory-semantics: Lease-takeover CAS verified correct, but takeover replays the in-flight task under the new claimant — a replay vector the call-log-hygiene arming blocker must also cover; aborted loser then does a failed upsert every poll
- low/pass-factory-semantics: getState().tasks calls writesArmed() unguarded — contract-level only; current consumers cannot throw
- high/nightly-and-render-seams: flattenFactUpdate $sets the whole coverage subdoc and emailAcceptedAt:null, so any snapshot re-write of an existing day permanently regresses callProjection/complete and erases the acceptance stamp
- medium/nightly-and-render-seams: No gate couples the 20:00 report scheduler to the 19:50 hygiene pass — the nightly email composes mid-pass and reads mail spend / spend sheet the pass has not produced yet
- medium/nightly-and-render-seams: No production writer can ever set coverage.callProjection to "complete", so isFactComplete is permanently false for every fact the integrated night writes
- medium/comment-claims-audit: Trio comment claims fields-sync staleCheckMs is 20h; the constant is 30 days
- low/comment-claims-audit: pacificMsSinceToday docstring's 'Nothing needs that today' is stale — the midday pass now uses the DST-divergent case it rules out
- low/comment-claims-audit: 'forceDaily bypasses that WINDOW and nothing else' is false — it also bypasses the ACTIVE_WEEKDAYS gate
- low/comment-claims-audit: Jira webhook header still documents 'acknowledge before work'; the code now claims before acking and 500s on claim failure
- low/comment-claims-audit: 'Eleven plan() calls a night' — the registry now has fifteen tasks
- high/test-mutation: Jira claim-drain CAS predicate shape is unverified — CAS on _id alone passes 36/36
- high/test-mutation: recordLink terminal-created guard: swapping the ternary branches passes 36/36
- high/test-mutation: No test executes the webhook handler — dropping `await` on claimIssue passes 36/36 while the route is completely broken
- high/test-mutation: The claim's core mutual-exclusion (in-flight lease refusal) has no test — instant-steal mutation passes 26/26
- high/test-mutation: drainCounterCadenceSweep loop tested only by source regex — falsifying `drained` passes 20/20 and defeats the factory's did-not-drain retry
- medium/test-mutation: fakeModel compares claim ownership via String(Date) — second precision vs Mongo's milliseconds; it is what let the no-renewal mutation survive
- medium/test-mutation: Midday hygiene's 20:00-yesterday anchor straddles both DST transitions — 1-hour coverage gap (fall) and 1-hour replay-unsafe overlap (spring); tests pin only the noon same-day case
- medium/test-mutation: Held-head fix has a silent cliff at ~1,000 episodes: 20-page bound, restart-from-top every tick, no truncation signal
- low/test-mutation: Remaining source-text assertions verify presence, not reachability (lower-stakes cluster)